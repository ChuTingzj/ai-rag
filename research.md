# 当代 RAG 系统调研：现状、痛点与下一代趋势

> 调研时间：2026-09  
> 范围：学术综述、工业实践与主流工程范式（2020–2026）  
> 结论先行：**Naive RAG（切块 → 向量检索 → 塞进 Prompt）已过时**；瓶颈从「生成能力」转向「检索精度、证据编排、权限与评测」。下一代 RAG 正在从「单次检索管道」演进为「可编排的知识基础设施」——Hybrid + Rerank 是生产基线，GraphRAG / Agentic RAG / Adaptive RAG 是复杂场景的扩展层。

---

## 1. 执行摘要

Retrieval-Augmented Generation（RAG）自 2020 年 Lewis 等人提出以来，已成为缓解大模型**知识过时**与**幻觉**的主流范式：把「参数记忆」与「外部非参数记忆」解耦，在推理时按需检索证据再生成。

到 2025–2026，行业共识大致如下：

| 维度 | 现状判断 |
| --- | --- |
| 技术成熟度 | 从 Demo 级 Naive RAG → Advanced / Modular / Agentic RAG；混合检索 + 重排已成为生产默认 |
| 能力天花板 | 即便 SOTA RAG，事实问答正确率仍约 **63%**；Naive RAG 约 **44%**（CRAG 等基准） |
| 核心瓶颈 | 检索失败、切块失联、噪声上下文、评测不充分、企业权限与数据新鲜度 |
| 长期定位 | RAG **不会被长上下文完全取代**；二者互补，路由取决于模型能力、语料规模、成本与任务类型 |
| 下一代方向 | 结构化检索（Graph）、自适应/智能体编排、多模态与实时数据虚拟化、因果式证据度量、检索层 ACL |

---

## 2. 现今 RAG 系统现状

### 2.1 演进路径（简表）

```
Naive RAG (2020–2023)
  └─ 固定切块 + 稠密向量 Top-K + Prompt 拼接

Advanced / Modular RAG (2023–2025)
  └─ 查询改写 / HyDE、混合检索、重排、上下文压缩、Parent-Child、Contextual Retrieval

Agentic / Graph / Adaptive RAG (2024–2026)
  └─ 多步规划检索、知识图谱与社区摘要、按需决定是否检索、工具调用与闭环校正
```

学术与工业文献普遍将发展弧线总结为：**单阶段 retrieve-then-read → 模块化管道（混合检索、自适应重排、自反思生成）→ 智能体驱动的检索行动规划**。

### 2.2 主流架构分层

当代生产级 RAG 通常拆成四层（Indexing / Retrieval / Fusion / Generation）：

1. **索引层（Indexing）**  
   - 解析与清洗（PDF/表格/多模态）  
   - 切块策略：语义切块、句子窗口、父子文档（小块检索 / 大块生成）  
   - 嵌入与稀疏索引（BM25 / SPLADE 等）  
   - 元数据富化（来源、时间、ACL、实体）  
   - 可选：知识图谱构建、层级摘要（如 RAPTOR）、Contextual Embedding（切块前注入文档语境）

2. **检索层（Retrieval）**  
   - **混合检索**：稠密语义 + 稀疏精确匹配，再用 RRF（Reciprocal Rank Fusion）融合  
   - 多查询 / RAG-Fusion、查询分解、HyDE（假设文档嵌入）  
   - 图检索 / 社区摘要检索（GraphRAG 类）  
   - 实时源：API / 数据虚拟化（不全依赖离线 ETL）

3. **融合与精排（Fusion / Rerank）**  
   - Cross-Encoder / ColBERT 等对 Top-N（如 50–100）精排，保留 Top-k（如 5–10）  
   - 上下文压缩、去重、冲突检测  
   - 权限过滤必须发生在**检索层**，而非仅应用层

4. **生成与编排（Generation / Orchestration）**  
   - 带引用的生成、忠实度约束、Guardrails  
   - Self-RAG / CRAG：反思、纠错、必要时回退到网络或其他源  
   - Agentic：多工具、多跳、中途再检索  
   - Adaptive：按查询复杂度路由「不检索 / 单次 / 多步」

### 2.3 2026 年的「生产基线」实践

多家工程实践与综述收敛到相近的默认栈：

| 能力 | 常见做法 | 作用 |
| --- | --- | --- |
| 切块 | 语义切块 + Parent-Child / Sentence Window | 兼顾检索精度与生成上下文 |
| 检索 | Dense + BM25 + RRF | 同时覆盖「意思相近」与「专有名词/编号」 |
| 精排 | Cross-Encoder（Cohere Rerank、BGE-Reranker 等） | 压噪声、缓解 Lost-in-the-Middle |
| 语境增强 | Anthropic Contextual Retrieval（块级上下文 + BM25） | 报告可降低检索失败约 **67%** |
| 评测 | RAGAS、忠实度/引用对齐、领域 golden set | 从「感觉不错」转向分量评测 |
| 安全 | 检索层 ACL、审计日志、LLM 无关架构 | 企业上线硬门槛 |

**重要共识：** 在 GPT / Claude / Gemini 等强模型时代，**失败点几乎总是检索**——漏掉表格里的关键一句、把 2024 与 2025 语义混淆、或把无关段落塞进上下文，都会直接表现为「幻觉」。

### 2.4 生态与落地形态

- **框架**：LangChain、LlamaIndex、Haystack、自研编排层  
- **向量库**：FAISS / HNSW、各类托管向量数据库；稀疏+稠密混合索引成为标配能力  
- **企业场景**：知识门户、客服、销售情报、合规问答、研发文档助手；金融/医疗/法律等高监管行业要求可追溯引用与权限隔离  
- **与微调关系**：RAG 负责「可变、可审计的外部知识」；微调/领域适配负责「风格、格式、领域语言」——多数生产系统二者并存，而非二选一

### 2.5 RAG vs 长上下文（Long Context）

长窗口（128K–百万级 Token）引发「还要不要 RAG」的争论。LaRA 等基准的结论是：

- **没有银弹**：最优选择取决于模型能力、上下文长度、任务类型与检索质量  
- 强模型 + 中等规模文档：纯长上下文有时更强（自注意力对完整上下文的利用更好）  
- 弱/中小模型、超大规模语料、成本敏感、需要引用与权限过滤：**RAG 仍不可或缺**  
- 实务上越来越多系统采用 **路由**：简单/短文档走 LC，大规模/跨库/需权限走 RAG，复杂题走 Agentic

---

## 3. 当下痛点（按严重程度与出现频率）

### 3.1 检索质量与「错误证据」

- 稠密向量对**精确实体、编号、日期、产品代码**不敏感，易语义近邻误召  
- Top-K 过小漏证据，过大引入噪声；噪声会稀释注意力并诱发胡编  
- 多跳问题（关系链、对比、汇总）单次向量检索天然不足  
- Hard negative 下，「相关性代理指标」与「生成是否真正用到该证据」经常脱节（因果度量研究指出的 diagnostic illusion）

### 3.2 切块与文档结构失真

- 固定长度切块切断段落、表格、条款逻辑  
- 孤立 chunk 丢失章节标题/文档主题 → Contextual Retrieval、父子索引要解决的正是这个问题  
- PDF/扫描件/复杂表格解析质量差，上游直接封顶下游准确率（「数据质量决定检索上限」）

### 3.3 幻觉并未消失，只是形态变了

- RAG **显著降低**但**不能消灭**幻觉：不完整/矛盾证据下仍会编造或错引  
- 引用幻觉：生成看似合法但并不存在的出处  
- 「检索到了但没用」或「用了错误段落」——与模型内部知识冲突时的 tug-of-war

### 3.4 评测与可观测性不足

- 端到端正确率掩盖组件故障（检索差 vs 生成差 vs 编排差）  
- 静态基准易污染/饱和；生产需领域 golden set + 分层诊断（检索召回、精排、忠实度、引用对齐）  
- 缺乏对「证据利用率」的因果式测量，优化常变成堆上下文

### 3.5 延迟、成本与复杂度

- 检索 + 重排 + 多轮 Agent 调用显著增加延迟与费用  
- 管道组件多：索引同步、嵌入版本、Prompt、权限、缓存——运维面爆炸  
- PoC 易做、企业级（TB 级语料、万级用户、复杂 ACL）难规模化

### 3.6 新鲜度与实时性

- 离线 ETL 索引天然滞后；金融/运维/库存等场景需要**查询时访问活数据**  
- 索引更新、增量重嵌入、向量与原文一致性是持续工程债

### 3.7 安全、隐私与合规

- 若 ACL 只做在应用层，RAG 会变成「权限绕过器」  
- Prompt injection、投毒文档、检索投毒（SafeRAG 等研究方向）  
- 输出侧泄密、日志中的敏感片段、跨境与数据驻留约束

### 3.8 结构化与多模态鸿沟

- 纯文本向量对「实体关系、层级政策、跨表关联」覆盖不足  
- 图/表/图文混排检索与统一融合仍不成熟，工程成本高

### 3.9 组织与产品层痛点

- 大量项目卡在 PoC：未解决权限、评测、ROI 与运维  
- 业务方期望「万能问答」，实际需要意图路由与人工兜底  
- 知识治理（元数据、生命周期、权威源）缺位时，算法优化收益有限

---

## 4. 下一代 RAG 趋势

### 4.1 从管道到「知识操作系统」

下一代系统不再是「一个向量库 + 一个 Prompt」，而是：

- **多索引联邦**：向量 / 关键词 / 图谱 / SQL / API / 网页  
- **策略路由**：Adaptive / Agentic 决定检索深度与工具  
- **闭环校正**：生成后验证、对比解码、不足则再检索  
- **统一治理**：权限、血缘、评测、成本预算贯穿全链路

### 4.2 Agentic RAG（智能体化检索）

- 模型规划：分解问题 → 选择工具/语料 → 中途再检索 → 综合作答  
- 适合多跳推理、调研类、跨系统工作流  
- 代价：成本、延迟、安全面扩大；需要过程级评测与护栏  
- 趋势是「可控智能体」：有限步数、可审计轨迹、预算感知

### 4.3 GraphRAG 与结构化检索

- Microsoft GraphRAG 等路线：实体抽取 → 图谱 → 社区摘要 → 全局/局部查询  
- 优势：关系型、汇总型、「全局主题」问题；补向量检索的结构性盲区  
- 代价：构图与维护贵、图谱质量敏感；简单事实问答可能过重  
- 演进方向：**Hybrid GraphRAG**（向量召回 + 图遍历 + 摘要融合）按需启用

### 4.4 Adaptive / Self-correcting 检索

- Self-RAG、CRAG、Adaptive RAG、FLARE 等：按需检索、评估证据、失败回退  
- 新研究强调：盲目多检索会引入噪声；应用**自适应 listwise 排序/深度**在准确率与成本间折中  
- 对强推理模型，RAG 更多扮演**效率与权限过滤器**，而非唯一正确性来源

### 4.5 证据度量与上下文预算科学化

- 从「堆更多 Token」转向**因果式 leave-one-out**等测量：哪段证据真正被生成依赖  
- 闭环编排：迭代多轮生成 + 强制引入新证据，优于一次性塞满上下文  
- 「上下文分配定律」类工作推动：把 context budget 当作可优化资源，而非越大越好

### 4.6 Contextual & Hierarchical Indexing 常态化

- Contextual Retrieval、RAPTOR 树摘要、层级索引成为长文档标配思路  
- 小块检索精度 + 大块/摘要生成上下文的模式会继续扩散

### 4.7 实时数据虚拟化与「活知识」

- 企业侧强调：CRM/ERP/风控等系统**查询时直连**，减少陈旧索引  
- RAG 层变为「编排 + 缓存 + 权限」的语义访问平面，而不只是文档搜索

### 4.8 多模态 RAG

- 图文表音视频统一检索与引用  
- 需要新的切块单元、对齐表示与融合算子；垂直领域（工业图纸、病历影像、设计稿）先落地

### 4.9 安全 RAG 与合规默认

- 检索层细粒度 ACL、行级/文档级权限、水印与溯源  
- 对抗投毒与注入的「攻击感知检索器」  
- 隐私计算 / 本地部署 / LLM-agnostic 成为采购默认要求

### 4.10 评测体系升级

- 从单一 QA 分数 → **分层基准**（检索 / 精排 / 生成忠实度 / 多跳 / 权限隔离）  
- 过程奖励、组件级诊断、生产线上反馈闭环  
- 领域专属评测集取代通用榜单作为上线门禁

### 4.11 与记忆系统、微调的融合

- 会话记忆 / 长期用户记忆 / 组织知识库分层  
- RAG（外置、可更新、可审计）+ 参数化记忆（偏好与技能）协同  
- 「搜索式生成」与「助手式 Agent」产品边界模糊，RAG 成为 Agent 的默认工具之一

---

## 5. 架构选型建议（实务）

| 场景 | 建议起点 | 何时升级 |
| --- | --- | --- |
| 内部文档问答、FAQ | Hybrid + Rerank + Parent-Child + 基础评测 | 召回差 → Contextual / 查询改写 |
| 含大量专有名词、编号 | 必须上 BM25/稀疏 + 元数据过滤 | — |
| 多跳关系、全局总结 | Hybrid 不够时再上 GraphRAG / RAPTOR | 图谱维护成本可接受时 |
| 查询复杂度混杂 | Adaptive 路由（不检索 / 单次 / 多步） | 成本敏感时优先 |
| 调研/工作流 | Agentic RAG + 工具调用 | 配步数/预算/审计 |
| 强监管企业 | 检索层 ACL + 引用强制 + 审计 | 同步数据治理 |
| 语料不大且模型很强 | 评估长上下文直读 vs RAG | 用 LaRA 类思路做路由实验 |

**优化顺序建议（高 ROI）：**  
数据与解析质量 → 切块与元数据 → Hybrid 检索 → Rerank → 查询改写/Contextual → 再考虑 Graph / Agent。

---

## 6. 总结

1. **现状**：RAG 已从实验特性变为企业 GenAI 的知识底座；生产默认是 **Hybrid + Rerank + 可观测评测**，而非「只上向量库」。  
2. **痛点**：准确率仍有硬顶；检索与切块、噪声、评测、权限、新鲜度、成本是主要拦路虎；幻觉问题转为「证据错误/滥用」形态。  
3. **趋势**：下一代是 **可路由、可验证、可治理的多智能检索系统**——Agentic 编排、Graph/结构化检索、自适应深度、实时源接入、因果式上下文分配与安全默认。  
4. **战略判断**：长上下文不会消灭 RAG；RAG 的价值从「塞进更多字」升级为「在正确权限下，把正确证据、以正确预算、交给正确生成策略」。

---

## 7. 主要参考

### 学术与综述

- Lewis et al., *Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks* (2020) — RAG 奠基工作  
- Oche et al., *A Systematic Review of Key RAG Systems: Progress, Gaps, and Future Directions* (arXiv, 2025/2026) — 演进、企业挑战与未来方向系统综述  
- *LaRA: Benchmarking RAG and Long-Context LLMs* (ICML / arXiv:2502.09977) — RAG vs 长上下文无银弹  
- Self-RAG (ICLR 2024)、RAPTOR (arXiv:2401.18059)、CRAG 等 — 自校正与层级索引  
- *The Laws of Context Allocation* (arXiv:2608.23252) — 证据因果度量与闭环上下文编排  
- Adaptive / listwise ranking 相关工作（如 2026 年 Adaptive RAG 必要性再思考）— 按需检索与成本权衡  

### 工业与工程

- Anthropic, *Contextual Retrieval* — 块级语境 + BM25，显著降低检索失败  
- Microsoft Research, *GraphRAG* — 图谱与社区摘要支撑全局/多跳问答  
- Squirro 等企业视角（2026）：实时数据虚拟化、知识图谱完整性、检索层 ACL 成为企业基线  
- 工程实践综述：Hybrid + RRF、Cross-Encoder 重排、Parent-Child 切块成为 2026 生产常识  
- Atlan 等对 Advanced RAG 技术的汇总（2026）：SOTA ~63% vs Naive ~44% 的准确率落差  

### 说明

本调研综合公开论文、技术博客与行业报告，侧重「可落地的共识判断」。具体数字随基准与领域变化较大，落地时应以**自有语料上的分层评测**为准，而非直接照搬榜单结论。
